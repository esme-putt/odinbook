<div id="<%= dom_id user %>">
  <p>
    <%= gravatar_for(user) %><br>
    <%= user.name %><br>
    <%= user.email %>
    <p>
        Followers: <%= user.followers.count %>
        Following: <%= user.following.count %>
    </p>
  </p>
</div>
